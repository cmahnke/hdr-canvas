{
  "dependencies": {
    "hdr-canvas": "git+https://github.com/cmahnke/hdr-canvas.git"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "postinstall": "cd node_modules/hdr-canvas && rollup --config"
    "build": "esbuild --bundle import.ts"
  }
}
